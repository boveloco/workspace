apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nullservers-tls
  namespace: cilium-gateway-system
spec:
  secretName: nullservers-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - nullservers.k8s.nullservers.com
