apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: collabora-tls
  namespace: cilium-gateway-system
spec:
  secretName: collabora-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - collabora.k8s.nullservers.com
