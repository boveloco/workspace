apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: duplicati-http-route
  namespace: kube-system
  annotations:
    external-dns.alpha.kubernetes.io/hostname: duplicati.k8s.nullservers.com
spec:
  parentRefs:
  - name: cilium-gateway
    namespace: cilium-gateway-system
  hostnames:
  - "duplicati.k8s.nullservers.com"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: duplicati
      port: 80